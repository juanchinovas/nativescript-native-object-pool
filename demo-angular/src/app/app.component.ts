import { NativeObjectPool } from 'nativescript-native-object-pool';
console.log(new NativeObjectPool().message);
import { Component } from "@angular/core";

@Component({
    moduleId: module.id,
    selector: "ns-app",
    templateUrl: "app.component.html"
})
export class AppComponent { }
